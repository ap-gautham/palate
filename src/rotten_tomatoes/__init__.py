"""Standalone Rotten Tomatoes critic-matched rating experiment.

Nothing in this package reads Letterboxd inputs or writes into its data,
results, or model directories. The two projects can therefore be run,
evaluated, and deployed independently.
"""
