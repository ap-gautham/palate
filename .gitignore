# Python
.venv/
__pycache__/
*.pyc
.DS_Store

# Data — Kaggle source (not redistributable) and regenerable processed parquets.
# Symmetric layout: each project keeps its own raw/ + processed/.
data/rotten_tomatoes/raw/
data/rotten_tomatoes/processed/
data/letterboxd/raw/
data/letterboxd/processed/

# Model binaries — regenerable by each project's training entry points.
results/rotten_tomatoes/models/
results/letterboxd/models/

# Scratch and LaTeX build artifacts
TRASH/
report/*.aux
report/*.log
report/*.out

# Web app -- node_modules and validation scratch; docs/ (the built site) is committed
web/node_modules/
web/js_validate_out.json
